#!/usr/bin/env perl

use strict;
use warnings;

my $value = 0;

print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
print ++$value, $/;
